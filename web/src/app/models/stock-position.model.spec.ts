import { StockPosition.Model } from './stock-position.model';

describe('StockPosition.Model', () => {
  it('should create an instance', () => {
    expect(new StockPosition.Model()).toBeTruthy();
  });
});
